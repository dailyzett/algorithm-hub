#include <bits/stdc++.h>
using namespace std;

int main() {
    double A, B;
    cin >> A >> B;

    cout.precision(12);  // 소수점 이하의 정확도를 높이기 위해 precision 설정
    cout << A / B << endl;

    return 0;
}